import { Rainbow } from './rainbow.directive';

describe('Rainbow', () => {
  it('should create an instance', () => {
    const directive = new Rainbow();
    expect(directive).toBeTruthy();
  });
});
